UNFOLD_APPS = [
    "unfold",
    "unfold.contrib.filters",
    "unfold.contrib.forms",
    "unfold.contrib.inlines",
    # The contrib apps below only make sense once their underlying package is
    # installed. Add the package to pyproject.toml first, then uncomment.
    # "unfold.contrib.import_export",   # requires django-import-export
    # "unfold.contrib.guardian",        # requires django-guardian
    # "unfold.contrib.simple_history",  # requires django-simple-history
    # "unfold.contrib.location_field",  # requires django-location-field
    # "unfold.contrib.constance",       # requires django-constance
]
